为什么两个线程都丢失了键，而不是一个线程？确定可能导致键丢失的具有2个线程的事件序列。

    为什么会造成数据丢失：假设现在有两个线程T1和T2，两个线程都走到put函数，
    且假设两个县城中key%NBUCKET相等，即要插入同一个散列通中。两个线程同时调用
    insert(key,value,&table[i],table[i]),insert是通过头插法实现的。如果先
    insert的线程还未返回另一个线程就开始insert,那么前面的数据将会被覆盖

    因此只需要对插入操作上锁即可